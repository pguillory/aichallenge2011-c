todo:
move to a friendly hill is never allowed
fix corner combat bug
log hills (remaining/total), ants (live+standby, dead)
avoid walls when fleeing.. rank moves by wall score
avoid killing the last enemy, cap hills instead
